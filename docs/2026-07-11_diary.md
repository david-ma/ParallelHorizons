# Gallery repository review — 2026-07-11

First-pass review of the **gallery** repo: a Thalia-hosted 3D WebGL art gallery derived from OwlSketch’s original Met Museum scraper concept, now maintained as David Ma’s exhibition digital twin and a teaching example for CDS2704.

---

## Executive summary

The project is in a **solid mid-transition state**. The Thalia conversion, TypeScript modularisation, Three.js r183 upgrade, Rapier physics, floorplan editor, and JSON-driven layout pipeline are largely done and working. What remains is mostly **productisation**: schema contract, async loading, auth/uploads, persistence, placards, and cleaning up legacy assets and docs.

Typecheck passes (`bun run typecheck`). Uncommitted work in the working tree: `package.json` (Thalia dep + scripts) and `bun.lock`.

---

## What this repo is

| Layer | Role |
|-------|------|
| **Thalia** | Serves pages, compiles TS on demand in dev, static assets from `public/` |
| **Handlebars** | `index.hbs`, `gallery.hbs`, `gallery_creation.hbs`, partials |
| **Client TS (~1,930 lines in `src/js/`)** | Three.js viewer, movement, physics, layout builder |
| **Static assets** | 30 demo artworks (`public/img/Artworks/`), floor texture, spotlight GLB, legacy JS |

**Routes** (`config/config.ts`):

- `/` — homepage with link to demo gallery
- `/view` — pointer-lock 3D walkthrough
- `/create` — D3 floorplan editor (dev tooling)
- `/dev-spotlight-slider` — serves spotlight tuning partial

**Run locally** (from Thalia monorepo):

```bash
cd /usr/local/dev/Thalia && bun run bin/develop.ts gallery
```

---

## Architecture (viewer)

The viewer is well split compared to the original monolithic `main.js`:

```
main.ts          → boot, pointer lock, render loop, physics vs legacy branch
layout.ts        → load floorplan JSON, build walls + artworks
gallery.ts       → hardcoded default scene (30 paintings + one spotlight rig)
artwork.ts       → frames and moulding
spotlight.ts     → GLB fixture + dev sliders + export JSON
movement.ts      → WASD / gamepad, camera-relative velocity
physics.ts       → Rapier floor, walls, player body
types.ts         → Gal, FloorplanBlob
```

**Movement:** WASD and analog stick use camera-relative forward/right on the XZ plane (`movement.ts`). Correct use of `dir × up` for right vector is documented in AGENTS.md.

**Physics:** Rapier initialises async; colliders are built from `wallGroup` meshes after scene creation. Active only when pointer lock is on (`usePhysics = physicsWorld && playerBody && !screensaver`). Screensaver mode keeps legacy manual AABB sliding against walls inside `main.ts`’s render loop.

**Layout modes:**

1. If `GET /gallery-floorplan.json` succeeds → `buildSceneFromFloorplan`
2. Else → `buildDefaultGallery` (hardcoded room, 30 images, featured spotlight)

There is **no `public/gallery-floorplan.json` in the repo** right now (latest commit: “Go back to default floorplan”), so `/view` always uses the default layout unless a file is added manually.

---

## Architecture (create page)

`/create` is a self-contained floorplan editor:

- 5×5 grid; click cells to activate/deactivate
- Drag photos onto wall bands (north/east/south/west)
- **One photo per wall** in the editor UI
- Export / import / download JSON with `version: 2`
- Charcoal Studio dark palette, scrollable photo sidebar
- D3 v7 loaded from CDN; logic in `gallery_creation.ts`

Mock catalog: 12 photos pointing at `/img/Artworks/0.jpg` … `11.jpg`.

---

## JSON schema — current contract (informal)

Editor exports (`gallery_creation.ts`):

```json
{
  "version": 2,
  "grid": { "rows": 5, "cols": 5 },
  "activeCells": ["2,2"],
  "placements": {
    "2,2": { "north": "photo-0", "east": "", "south": "", "west": "" }
  },
  "photoCatalog": [{ "id": "photo-0", "title": "Photo 1", "src": "/img/Artworks/0.jpg" }]
}
```

Viewer consumes (`types.ts` / `layout.ts`):

- Same shape, but **viewer accepts `string | string[]` per wall** and can place **multiple artworks per wall** with horizontal spacing
- Backward compat: whole-cell string or legacy shapes normalised on import in the editor
- `photoCatalog[].title` is typed but **not rendered** (placards not implemented)

**Gap:** AGENTS.md changelog mentions “multi-photo per cell” on the viewer side, but the editor was simplified to one photo per wall. The schema is not formally documented or version-validated beyond the editor’s `version: 2` field.

---

## Strengths

1. **Clear module boundaries** — easy to navigate for agents and humans; AGENTS.md is an unusually good handoff doc.
2. **Modern stack** — Three.js 0.183.2 via npm, strict TypeScript, Rapier for collision.
3. **Dual layout path** — JSON floorplan + sensible fallback keeps `/view` usable without setup.
4. **Dev ergonomics** — spotlight tuning sliders (localhost only), JSON export, floorplan editor.
5. **Art presentation** — JSON mode gets black frame + white moulding; overlap bug was fixed.
6. **Pointer-lock UX** — Esc / blur / visibility release to menu; re-entry via PLAY overlay.

---

## Issues and technical debt

### Documentation drift

- **README.md** todo list is stale (still lists “refactor main.js to TypeScript” and “Three.js r183” as future work — both done).
- README quickstart is accurate; AGENTS.md is more current than README.

### Loading and lifecycle

- **`loadFloorplan` uses synchronous XHR** (`layout.ts`) — blocks the main thread; AGENTS.md already flags async load + loading state as near-term work.
- Textures and GLB load via `TextureLoader` / `GLTFLoader` without central loading state or error UI.

### Feature gaps (viewer vs default)

- **Spotlights only in default gallery** — JSON-built scenes get ambient + hemisphere light only; no per-artwork rigs.
- **No placards** — title/artist/year in schema but not shown in 3D.
- **Raycast / painting selection** — legacy path still raycasts in render loop, but selection UI from original OwlSketch alpha may be incomplete in Thalia templates.

### Code quality notes

- **`main.ts` is still the largest file (375 lines)** — render loop mixes physics step, legacy collision, wall colour reset, and framerate; candidate for extraction.
- **`Gal` uses `(window as any)` / `(g as any)` in places** — workable but weakens strict typing.
- **`gallery_creation.ts` declares `d3: any`** — fine for a dev tool, could use `@types/d3` if tightened.
- **jQuery** on `gallery.hbs` only for `#gamepadtest` dev toggle — could be vanilla JS.

### Legacy / duplicate assets

Still present under `public/` from pre-Thalia era:

- `public/index.html`, `public/js/three.js`, `three.min.js`, `three.module.js`
- `public/js/PointerLockControls.js`, `newPointerLockControls.js`
- `public/js/GLTFLoader.js` (viewer now imports from `three` npm package)
- `system_programs/python/scraper.py`, `newscraper.py`, `cpp/minAlgo.cpp`

These are likely **unused** in the Thalia flow but add noise and confusion for new contributors.

### Testing and CI

- No test script (removed placeholder `test` echo in uncommitted `package.json`).
- No automated checks beyond manual typecheck.

### Uncommitted changes

```
package.json  → start/dev/typecheck scripts, thalia github dependency
bun.lock      → lockfile update
```

Worth committing when the Thalia dependency pin is intentional.

---

## Asset inventory

| Asset | Status |
|-------|--------|
| `public/img/Artworks/*.jpg` (30) | Tracked; demo exhibition photos |
| `public/img/Textures/Floor.jpg` | Used by both layout modes |
| `public/models/spotlight/Spotlight.glb` | CC0 Poly Pizza model |
| `public/gallery-floorplan.json` | **Missing** — viewer falls back to default |
| `public/img/OldArtworks/` | Legacy Met scraper metadata + images |

---

## Roadmap alignment (from AGENTS.md)

| Near-term item | Status |
|----------------|--------|
| Stabilise JSON schema + document contract | Partial — types exist, no formal schema doc |
| Async floorplan load + loading state | Not done |
| JSON build in `layout.ts` | Done |
| Apply one spotlight tuning to all fixtures | Not done; JSON mode has no spotlights |
| Placards (title/artist/year) | Not done |

| Future phase | Notes |
|--------------|-------|
| **A — data model** | `Artwork` class, schema migrations |
| **B — user features** | Auth, dashboard, UploadThing, DB persistence |
| **C — richer scenes** | Props, object collision, share links, audio |

Homepage already telegraphs “log in to create your own (coming soon).”

---

## Suggested next steps (priority order)

1. **Commit `package.json` / `bun.lock`** if Thalia-as-dependency is the intended dev path.
2. **Add a sample `public/gallery-floorplan.json`** (or document that users must download from `/create`) so JSON mode is testable out of the box.
3. **Write a short schema doc** (`docs/floorplan-schema.md` or section in AGENTS.md) aligning editor output with viewer expectations, including `version` and migration notes.
4. **Replace sync XHR** with `fetch` + loading overlay in `/view`.
5. **Update README.md** to match current state (done items, `/create` route, physics, floorplan flow).
6. **Archive or delete legacy `public/js/` Three bundle** if confirmed unused by Thalia routes.
7. **Spotlights in JSON mode** — even a single template rig copied per wall would improve parity with the default gallery.

---

## Git history (recent)

```
c3eb5cd Go back to default floorplan
b5cc5e2 Improve gallery creation
b0416a0 Improve movement
3fe80b0 Use Rapier for physics
c60bb92 Try improving player collision
6e08f0c Split up the logic into different files
```

Recent work cluster: modularisation → collision/physics → floorplan editor → revert committed floorplan JSON.

---

## Bottom line

This is a **credible foundation for a user-generated 3D gallery product**, not just a demo. The hard parts — Thalia integration, TS refactor, physics, and layout authoring — are largely solved. The gap to “users upload photos and share a walkable gallery” is mostly **backend, schema formalisation, and polish** (loading states, spotlights, labels, mobile controls), not a rewrite of the 3D core.

---

## Session notes — 2026-07-11 (afternoon)

### Triplex decision

[Triplex](https://triplex.dev/) targets **React Three Fiber** visual editing. Our stack is vanilla Three.js + Thalia + JSON floorplans. **Not adopting Triplex** without a full R3F rewrite. Better to extend `/create` and the JSON schema.

### Interview answers (build priorities)

| Q | Choice |
|---|--------|
| Work bundle | Core JSON path: sample floorplan + schema doc + async load + loading UI |
| Missing/invalid JSON | **Minimal empty room** (floor only) — not silent fallback to hardcoded exhibition |
| JSON spotlights | **One rig per artwork** |

### Build todo list

- [x] Restore `public/gallery-floorplan.json` (sample layout from git history)
- [x] Add `docs/floorplan-schema.md`
- [x] Replace sync XHR with `loadFloorplanAsync()` + validation
- [x] Loading overlay on `/view` while layout fetches
- [x] `buildMinimalGallery()` when JSON missing/invalid
- [x] Per-artwork spotlights in JSON mode (`spotlightOptionsForArtwork`)
- [x] Update README (routes, schema link, current status)
- [ ] Commit `package.json` / `bun.lock` (when ready)
- [x] Placards (title/artist/year) in viewer
- [ ] Phase B: auth, UploadThing, DB persistence
- [ ] Legacy asset cleanup (`public/js/` old Three bundles)
- [ ] Optional: route or query param to load hardcoded `buildDefaultGallery()` exhibition

### Implementation notes

- `main.ts`: `create()` is now async; Rapier init runs after layout load completes.
- `layout.ts`: shared floor/lighting helpers; sync `loadFloorplan` removed.
- `spotlight.ts`: `spotlightOptionsForArtwork()` derives rig from artwork position/rotation.
- Hardcoded exhibition (`gallery.ts` / `buildDefaultGallery`) remains in repo but is no longer the `/view` fallback.

---

## Session notes — 2026-07-11 (floorplan editor UX)

### `/create` editor: drag-move + undo/redo

**Problem:** Deactivating a cell deletes all artworks on that cell with no recovery. Placed thumbnails could not be repositioned on the grid except by re-dragging from the sidebar.

**Planned behaviour:**

| Action | How |
|--------|-----|
| Move artwork on grid | Drag a placed thumbnail on `#floorplan-svg` to another wall band (same or different cell). Swaps if the target wall already has a photo. |
| Drag from sidebar | Unchanged — drop onto a wall band to place (copy). |
| Undo | `Ctrl+Z` / `Cmd+Z` — cell activate/deactivate, placements, moves, placard snapshot |
| Redo | `Ctrl+Y` or `Ctrl+Shift+Z` / `Cmd+Shift+Z` |
| Shortcuts disabled | While focus is in a placard `<input>` (so typing isn't intercepted) |

**Implementation approach:**

- Snapshot editor state (`activeCells`, `placements`, `photoCatalog`, selection) before each mutating action.
- `application/x-gallery-from` drag payload marks moves from the grid vs new drops from the photo list.
- History stack capped (~100 entries); import/load/reset wrapped in `mutate()` so they are undoable.

**Shipped:** `gallery_creation.ts` — `mutate()` / `undo()` / `redo()`, draggable `.placed-art` SVG images with `application/x-gallery-from` payload, wall-drop swap logic, placard edit batches one undo step per focus session.

### Build todo (editor UX)

- [x] Drag placed artworks between wall zones on SVG
- [x] Undo/redo stacks for layout mutations
- [x] Update `/create` hint text + diary (this section)

---

## `/create` UX ideas (backlog)

The editor works but still feels like a dev tool: cramped sidebar, tiny grid thumbnails, hidden affordances, and a manual export → save → view loop. Ideas below, roughly ordered by impact vs effort.

### High impact — layout & safety

1. **Confirm destructive actions** — Deactivating a cell or Reset should show a small confirm dialog (“Remove 4 artworks from this room?”). Undo helps, but prevention is better UX than recovery.

2. **Separate “remove artwork” from “deactivate cell”** — Clicking a cell toggles the whole room off. Better: right-click cell → Deactivate room; click placed art → select; **Backspace** or trash icon → remove from wall only. Keeps layout editing from feeling like a trap.

3. **Bigger placed thumbnails + wall affordances** — 14px thumbs are hard to grab. Show ~32px previews on walls, faint “N/E/S/W” labels on bands, and a stronger hover state so drop targets are obvious before drag starts.

4. **“Save & preview” one click** — POST or write `gallery-floorplan.json` server-side (Thalia route), then open `/view` in a new tab. Removes the export/download/copy ritual entirely.

5. **Tabbed or accordion sidebar** — Today: controls + raw JSON + hint + placard + photos in one scroll column. Split into **Layout | Photos | Labels** tabs so the grid gets more horizontal space and JSON moves behind “Advanced”.

### Medium impact — clarity & feedback

6. **Placed vs unplaced photos** — Grey out or badge photos already on a wall; filter “Unplaced only” so the library doesn’t feel endless when you have 12 mocks.

7. **Selection model on the grid** — Single-click thumbnail = select (placard panel updates); double-click = edit placard. Cell background click = select room, not instant deactivate (require second action or modifier key).

8. **Undo/redo UI** — Toolbar buttons + disabled state when stack empty; brief toast (“Undid: deactivated cell 3,1”) so keyboard shortcuts are discoverable.

9. **Live minimap or 3D peek** — Small embedded `/view` iframe or static isometric preview that updates on change (debounced). Helps users who don’t think in top-down grid coordinates.

10. **Drag ghost** — Custom drag image (full photo tile) instead of default browser ghost; show valid drop zones highlighted for the duration of the drag.

11. **Empty-state onboarding** — First visit: “1. Click cells to add rooms → 2. Drag photos to dashed edges → 3. Save & preview”. Dismissible; link to schema doc.

### Polish & power-user

12. **Paint tool for cells** — Brush mode: click-drag to activate/deactivate multiple cells instead of one-by-one toggling (with undo batches).

13. **Context menu on placements** — Right-click artwork on grid: Edit label, Remove, Swap with…, Jump to photo in library.

14. **Keyboard shortcuts cheat sheet** — `?` opens modal: Z/Y undo, Delete remove art, Esc clear selection.

15. **Collapsible JSON panel** — Default hidden; devs expand when needed. Frees ~180px vertical space for photos/placards.

16. **Rename route & homepage link** — `/create` → “Edit gallery” on `/`; drop “(Dev)” from title once save/preview exists.

17. **Mobile/tablet** — Touch drag on SVG is flaky; consider simplified flow (list of rooms → pick wall → pick photo) for narrow screens.

18. **Upload photos in-editor** — UploadThing picker in Photos tab; auto-add to `photoCatalog` instead of 12 hardcoded mocks.

### Longer term (product)

19. **User galleries** — Edit page at `/edit/:id` loads from DB; autosave; share link for read-only `/view/:slug`.

20. **In-world editing** — Pointer-lock in `/view`, aim at artwork, press E to edit placard (connect editor to 3D context).

### Suggested next slice (if picking one)

**#4 Save & preview + #5 tabbed sidebar + #3 bigger thumbs** — biggest perceived jump from “dev JSON toy” to “gallery editor” without a full rewrite.

---

## Performance, memory & telemetry

### What we know today (2026-07-11)

The viewer is lightweight by modern WebGL standards, but we have **almost no instrumentation** beyond a live FPS counter. This section records a first-pass estimate and a plan to capture real data.

#### Texture / RAM pressure (wall & ceiling change)

Procedural wall and ceiling textures (`materials.ts`) are two **512×512 RGBA** canvases:

| Asset | JS heap (approx.) | GPU VRAM (approx.) |
|-------|-------------------|---------------------|
| Wall source texture | ~1 MB | ~1 MB |
| Ceiling source texture | ~1 MB | ~1 MB |
| **Incremental cost vs flat white walls** | **~2 MB** | **~2 MB** |

That is negligible compared to the rest of the scene:

| Asset class | Notes |
|-------------|-------|
| Artwork JPGs | Dominant cost. Current floorplan has ~9 placements; each decoded GPU texture is often **1–4 MB** depending on resolution. Catalog on disk ~4 MB total; GPU decode can exceed file size. |
| Floor | `Floor.jpg` (512×512) — already loaded before wall/ceiling work. |
| Spotlight GLB | ~30 KB on disk, but `GLTFLoader.load()` is called **once per artwork** — redundant parses/clones, not yet cached. |
| Placards | One small `CanvasTexture` per labelled artwork. |
| Rapier | WASM physics heap on top of the above. |

JSON layout shares **one wall material** across all wall segments (good). Default hardcoded gallery creates four wall material instances but they reference the same canvas source.

#### FPS

Computed in the render loop (`main.ts` → `framerate()`), displayed in `#devonly`:

```
FPS: <span id="fps"></span>
```

Updated every ~15 frames. A **duplicate** updater exists in `gamepadtest.ts` (jQuery, every 30 frames when a gamepad is connected).

There is **no shadow mapping**, which keeps GPU cost down. Per-artwork `SpotLight` count scales with placement count (currently ~9 in sample floorplan). Lambert/Phong materials, modest geometry — **60 FPS is the desktop target**, but we are not recording proof.

#### What we are *not* tracking

| Metric | Status |
|--------|--------|
| FPS history / percentiles | Not captured |
| RAM / JS heap | No `performance.memory`, no logging |
| GPU / texture memory | No `renderer.info` |
| Draw calls / triangles | No Three.js `Stats` panel |
| Load timings (JSON, textures, GLB, Rapier init) | Not captured |
| Persistence / analytics | Nothing sent or stored |

The diary and AGENTS.md note that `main.ts` still mixes framerate counting into the render loop — a refactor candidate, and a natural hook for a future `telemetry.ts` module.

### Observed browser differences (informal)

Subjective testing on the same machine and gallery layout:

| Browser | Notes |
|---------|-------|
| **Cursor built-in browser** | Noticeably **faster and smoother** — snappier pointer-lock feel, cleaner frame pacing during walk-through. |
| **Chrome** | Usable but feels heavier; more jank when moving through the corridor. |
| **Safari** | **Slowest** of the three tested — lower perceived FPS, less responsive movement. |

We do not yet have numbers to back this up. Likely contributors (hypotheses until instrumented):

- Different WebGL backends and compositor behaviour (Safari / Metal vs Chrome / ANGLE).
- Cursor's embedded browser may run with different GPU process isolation or vsync defaults.
- Spotlight count + texture upload path may hit Safari's stricter memory or shader limits sooner.
- No adaptive quality tier — same scene graph and light count everywhere.

**Action:** Treat browser/engine as a first-class dimension in telemetry, not an afterthought.

### Telemetry plan (future)

Goal: lightweight, privacy-conscious client telemetry so we can answer “how fast? how much RAM? on what setup?” without guessing.

#### Phase 1 — Dev overlay (localhost only)

Extend `#devonly` (or a `?debug=1` query param) with a collapsible panel:

| Field | Source |
|-------|--------|
| FPS (current + 1 s / 10 s avg) | Existing `framerate()` + rolling buffer |
| Frame time ms | `performance.now()` delta |
| Draw calls, triangles, textures, geometries | `renderer.info.render` / `renderer.info.memory` |
| JS heap used / total | `performance.memory` (Chrome/Edge; graceful omit elsewhere) |
| Scene counts | Artworks, walls, spotlights, colliders (from `gal`) |
| Load timings | Floorplan fetch, Rapier init, first frame rendered |

No network; on-screen only. Replaces duplicate FPS logic in `gamepadtest.ts`.

#### Phase 2 — Session snapshot (optional POST)

On `/view` load complete + on pointer-lock exit (or every N minutes while active), emit one **session snapshot** JSON:

```json
{
  "event": "gallery.session",
  "ts": "2026-07-11T06:13:00.000Z",
  "gallery": { "mode": "json", "artworks": 9, "activeCells": 16 },
  "perf": {
    "fpsAvg": 58,
    "fpsMin": 42,
    "frameTimeP95Ms": 22,
    "drawCalls": 140,
    "triangles": 8200,
    "textures": 12,
    "heapUsedMb": 85
  },
  "client": {
    "userAgent": "...",
    "browser": "Chrome",
    "browserVersion": "138",
    "engine": "Blink",
    "platform": "MacIntel",
    "deviceMemoryGb": 16,
    "hardwareConcurrency": 10,
    "maxTextureSize": 16384,
    "webglRenderer": "ANGLE (Apple, ANGLE Metal Renderer: Apple M1 Pro, Unspecified Version)",
    "webglVendor": "Google Inc. (Apple)",
    "dpr": 2,
    "viewport": { "w": 1920, "h": 1080 },
    "window": { "w": 2560, "h": 1440 },
    "pointerLockSupported": true,
    "language": "en-AU",
    "timezone": "Australia/Sydney"
  }
}
```

**Thalia route:** e.g. `POST /telemetry/gallery` — append to log file or DB table in dev; rate-limit; no PII beyond coarse UA-derived browser name.

#### Phase 3 — Product analytics (when auth exists)

- Tie snapshots to `userId` / `galleryId` for creator dashboards (“your gallery: median 54 FPS on mobile Safari”).
- Adaptive quality: if `fpsMin < 30` for 5 s, reduce spotlight count, texture max size, or disable placard canvas textures.
- A/B: procedural vs flat walls, spotlight-per-art vs shared rig.

#### Client environment fields to capture

| Category | Fields |
|----------|--------|
| **Display** | `window.innerWidth/Height`, `screen.width/height`, `devicePixelRatio`, `orientation`, fullscreen state |
| **Browser** | Parsed name/version from UA (or `navigator.userAgentData` where available), engine, mobile vs desktop heuristic |
| **System** | `navigator.platform`, `navigator.hardwareConcurrency`, `navigator.deviceMemory` (Chrome), `navigator.language`, `Intl` timezone |
| **WebGL** | `WEBGL_debug_renderer_info` vendor/renderer (with user consent note in privacy policy), max texture size, max vertex uniforms |
| **Capabilities** | Pointer lock, gamepad present, WebGL2 vs WebGL1, `OffscreenCanvas` support |
| **Network (optional)** | `navigator.connection.effectiveType` if available — useful for texture loading strategy |

#### Privacy & storage

- Default **opt-in** or anonymous-only until privacy policy covers it.
- Do not log raw IP in application payload; let reverse proxy handle geo if needed.
- Hash or truncate `userAgent` if storing long-term; keep parsed browser/OS columns for aggregation.
- Retention: 90 days for raw events; roll up to daily medians per browser × artwork-count bucket.

#### Build todo (telemetry)

- [ ] Extract `framerate()` + rolling stats into `src/js/telemetry.ts`
- [ ] Dev overlay: FPS avg/min, `renderer.info`, heap (Chrome)
- [ ] `collectClientInfo()` helper — UA parsing, viewport, WebGL caps
- [ ] `collectSceneStats(gal)` — artwork/wall/spotlight counts, load timings
- [ ] Thalia `POST /telemetry/gallery` route (dev: append JSONL to `logs/gallery-telemetry.jsonl`)
- [ ] Document schema in `docs/telemetry-schema.md`
- [ ] Safari / Chrome / Cursor comparison once Phase 1 overlay exists — record numbers in this diary
- [ ] Cache spotlight GLB load (single parse, clone per rig) — perf win independent of telemetry
- [ ] Consider Three.js `Stats` panel behind dev-mode toggle

#### Immediate takeaway

The wall/ceiling textures are **not** the performance story. **Artwork texture count, per-artwork spotlights, browser WebGL stack, and uncached GLB loads** are the levers worth measuring — and we currently have no data pipeline for any of it. Cursor's browser feeling best and Safari worst is a strong signal to prioritise cross-browser snapshots before optimising blindly.

**Update (same day):** Chrome's ~30 FPS cap on the same machine was traced to **Settings → Performance → Energy Saver**, not WebGL or our code. `chrome://gpu` showed hardware acceleration and a 120 Hz panel while rAF was still throttled. With Energy Saver off, Chrome matches expected ProMotion pacing.

---

## Session notes — 2026-07-11 (evening): editor polish, materials, dev UX

### `/create` editor — product slice

Shipped a batch of high-impact UX from the backlog:

| Feature | Notes |
|---------|-------|
| **Save & preview** | `POST /save-floorplan` writes `public/gallery-floorplan.json`, opens `/view` |
| **Tabbed sidebar** | Layout \| Photos \| Labels; JSON preview under Advanced |
| **Spawn marker** | Set start position on grid; persisted as `spawn.cell` |
| **Bigger wall thumbnails** | 28px on SVG; pointer-drag to move placements between walls |
| **Undo/redo** | `Ctrl+Z` / `Ctrl+Y`; wall-style changes included in history |
| **Placards** | Labels tab + 3D placards in viewer (`artwork.ts`) |
| **Wall texture picker** | Layout tab `<select>` → `wallStyle` in floorplan JSON (gallery-wide) |

Shared option list: `WALL_TEXTURE_OPTIONS` in `types.ts` (plaster, linen, concrete, silk).

### Procedural surfaces (`materials.ts`)

| Surface | Implementation |
|---------|----------------|
| **Walls** | Four procedural canvas textures; shared material across all wall segments in JSON mode |
| **Ceiling** | Tiled acoustic-panel grid (unchanged — user approved) |
| **Floor** | Existing `Floor.jpg` |

**Wall styles:**

- `plaster` — default; warm off-white, roller lap marks (~3 m repeat)
- `linen` — basket-weave canvas
- `concrete` — cool micro-cement; **~0.65 m repeat** (finer procedural detail + tighter UV scale)
- `silk` — vertical lime-wash bands

Viewer resolves style: `floorplan.wallStyle` → `?wallStyle=` query → `plaster`.

### Spotlight system (`spotlight.ts`)

Major rework for JSON + default galleries:

- **Wall-normal mounting** for all four orientations (replaced broken `wallZ`-only math)
- **Global tuning registry** — `SPOTLIGHT_TUNING` + localStorage; all rigs update together
- **Wall-aligned emitter disc** (+Y vertical, +Z into room); single pitch control
- **GLB fixture** — `FIXTURE_YAW_OFFSET = π` so model faces the wall
- **Dev panel** — grouped sliders; copy JSON / TypeScript; reset defaults
- **`initSpotlightDevPanel()`** after scene build in `main.ts`

User tuned and committed values in `SPOTLIGHT_TUNING` (e.g. `mountY: 4.5`, `intensity: 6`, `fixturePitchOffset: 1.6`).

### Dev / production UX (`gallery.hbs`, `index.css`, `config.ts`)

| Change | Behaviour |
|--------|-----------|
| **Pause menu centred** | Removed `body.dev-mode #menu` bottom-right pin |
| **Spotlight tuning behind button** | “Spotlight tuning” toggle on pause menu; panel hidden until clicked |
| **FPS overlay** | `#devonly` hidden unless `body.dev-mode` (localhost) |
| **Production gate** | `NODE_ENV === 'production'` → no FPS, no spotlight UI in HTML; `GALLERY_DEV_TOOLS` false; `initSpotlightDevPanel` / `framerate()` no-op |

### Physics & movement (unchanged this slice)

Rapier floor/walls/artwork colliders; camera-relative WASD; jump velocity in `physics.ts`. Render loop still branches physics vs legacy collision in `main.ts`.

### Schema & docs

- `docs/floorplan-schema.md` — spawn, placards, **`wallStyle`**, save/preview workflow
- `types.ts` — `WallTextureStyle`, `WALL_TEXTURE_OPTIONS`, `wallStyle?` on `FloorplanBlob`
- `public/gallery-floorplan.json` — sample layout with placements + spawn (in repo)

### Build todo (remaining)

- [ ] Commit accumulated changes (`package.json`, materials, spotlights, editor, dev UX)
- [ ] Cache spotlight GLB (single parse, clone per rig)
- [ ] Telemetry Phase 1 dev overlay (`docs/2026-07-11_diary.md` plan)
- [ ] Per-wall texture overrides (explicitly deferred — gallery-wide only for now)
- [ ] Phase B: auth, UploadThing, DB persistence

### Files touched (this session cluster)

| Area | Key paths |
|------|-----------|
| Editor | `gallery_creation.hbs`, `gallery_creation.ts`, `config/config.ts` |
| Viewer materials | `materials.ts`, `layout.ts`, `gallery.ts` |
| Spotlights | `spotlight.ts`, `spotlight_sidebar.hbs` |
| Dev UX | `gallery.hbs`, `public/css/index.css`, `main.ts` |
| Types / schema | `types.ts`, `docs/floorplan-schema.md` |

---

## 2026-07-11 — Product workflow preferences (David)

Captured for future agents:

| Preference | Detail |
|------------|--------|
| **User runs infra commands** | Docker, `db:migrate`, `db:seed`, MailCatcher, full `bun test` — agent **documents** steps, does not assume it ran them |
| **Migrations** | `drizzle-kit generate` + **`migrate`**, not `push` |
| **Integration tests** | Run by default; **fail loudly** if DB/MailCatcher missing; `SKIP_*=1` to opt out (CI uses `test:unit` only) |
| **Route guard quirk** | Thalia maps permission from URL **action segment**, not HTTP method — POST `/gallery-create` needs **`read`** on the route rule |
| **Scope discipline** | Note future UX (share layouts, rotate rooms, drag rooms) in docs; don’t build off-slice without ask |
| **Decisions** | Structured interview (multiple choice); encode answers like `1b, 2c` |
| **Docs** | `docs/2026-07-11_user_experience.md` for UX; diary for learnings |

**D2 interview (2026-07-11):** upload on library **and** editor sidebar; **strip deleted photos from all owner floorplans**; folders deferred to D4 (flat list).

**D2 shipped (2026-07-11):** `photos` table + `photo-store.ts` (owner-scoped list/insert/soft-delete + floorplan strip); `POST /uploadPhoto`, `GET /api/photos`, `POST /photo-delete/:id`, `/library` page; editor loads API catalog instead of mock `/img/Artworks/*`. Custom local-disk handler (not `ThaliaImageUploader` yet — D3 can swap adapter).
